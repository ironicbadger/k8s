apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Reference shared Flux bootstrap configuration
  - ../../common/bootstrap/flux
  # Cluster-specific sync configuration
  - gotk-sync.yaml
  - flux-system-secret.sops.yaml
