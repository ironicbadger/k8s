apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: taloslab-flux-webhook
  namespace: flux-system
  annotations:
    tailscale.com/funnel: "true"
spec:
  defaultBackend:
    service:
      name: webhook-receiver
      port:
        number: 80
  ingressClassName: tailscale
  tls:
    - hosts:
        - taloslab-flux-webhook